allOf:
  - $ref: '#/components/schemas/AccountObjectKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - email
          - access-token
          - expiry
          - verified
        properties:
          email:
            type: string
          access-token:
            type: string
          expiry:
            type: integer
          verified:
            type: boolean
